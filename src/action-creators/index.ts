import * as TournamentActionCreators from '../action-creators/tournaments';

// add it for future develop of the app as a big app with many different states
export default {
  ...TournamentActionCreators,
};
